<Window x:Class="MyJiraTickets.EditTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Ticket" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Ticket Key:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" x:Name="KeyBox" Margin="0,0,0,10" IsReadOnly="True" Background="LightGray"/>
        
        <Label Grid.Row="2" Content="URL:" FontWeight="Bold"/>
        <TextBox Grid.Row="3" x:Name="UrlBox" Margin="0,0,0,10"/>
        
        <Label Grid.Row="4" Content="Summary:" FontWeight="Bold"/>
        <TextBox Grid.Row="5" x:Name="SummaryBox" Margin="0,0,0,10" TextWrapping="Wrap" AcceptsReturn="True"/>
        
        <Label Grid.Row="6" Content="Status:" FontWeight="Bold"/>
        <ComboBox Grid.Row="7" x:Name="StatusBox" Margin="0,0,0,10" Height="25">
            <ComboBoxItem Content="To Do"/>
            <ComboBoxItem Content="Refinement"/>
            <ComboBoxItem Content="In Progress"/>
            <ComboBoxItem Content="Waiting for Deploy"/>
            <ComboBoxItem Content="In Test"/>
            <ComboBoxItem Content="Test Succeeded"/>
            <ComboBoxItem Content="Test Failed"/>
            <ComboBoxItem Content="Done"/>
            <ComboBoxItem Content="Canceled"/>
            <ComboBoxItem Content="On Hold"/>
        </ComboBox>
        
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="30" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
